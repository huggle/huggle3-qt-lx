#!/bin/sh

DEST=/

if [ "$#" -gt 0 ];then
  DEST=$1 
fi
if [ ! -f huggle ];then
  echo "FATAL: You need to build huggle first"
  exit 1
fi

echo "Installing huggle to $DEST"
if [ ! -d "$DEST"usr/bin ];then
  mkdir -p "$DEST"usr/bin
fi
cp huggle "$DEST"usr/bin/huggle || exit 1

echo "Installed"
