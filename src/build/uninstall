#!/bin/sh

DEST=/

if [ "$#" -gt 0 ];then
  DEST=$1
fi

echo "Uninstalling huggle from $DEST"

rm -fv "$DEST"usr/bin/huggle
rm -fv "$DEST"usr/share/applications/huggle.desktop
rm -fv "$DEST"usr/share/man/man1/huggle.1.gz
